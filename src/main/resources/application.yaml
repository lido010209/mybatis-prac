spring:
  datasource:
    url: jdbc:sqlite:db.sqlite
    driver-class-name: org.sqlite.JDBC


mybatis:
  mapper-locations: classpath:mybatis/mappers/*.xml
  type-aliases-package: com.example.mybatis.logup
  configuration:
    map-underscore-to-camel-case: true